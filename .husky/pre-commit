#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run type checking
echo "🔍 Running type check..."
npx tsc --noEmit || exit 1

# Run linting on staged files using lint-staged
echo "🧹 Running lint on staged files..."
npx lint-staged || exit 1

echo "✅ Pre-commit checks passed!"
